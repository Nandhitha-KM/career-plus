package com.careerplus.model;

public enum PriorityLevel {
    HIGH,    // Score >= 70 (Immediate Follow-up Required / Overdue)
    MEDIUM,  // Score 40-69 (Prepare for Interview / Action Soon)
    LOW      // Score < 40 (Informational / No Action Needed)
}
